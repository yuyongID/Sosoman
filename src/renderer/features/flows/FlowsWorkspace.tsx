import React from 'react';

/**
 * Placeholder workspace component for orchestrating test flows.
 *
 * The expectation is that this module will eventually manage complex state,
 * hence keeping it isolated from other features.
 */
export const FlowsWorkspace: React.FC = () => {
  return (
    <section>
      <h2>Flows</h2>
      <p>Flow designer UI goes here.</p>
    </section>
  );
};
