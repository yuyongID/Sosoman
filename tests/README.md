# Tests

This directory is intentionally left as a placeholder. As features stabilise,
unit tests (Vitest/Jest) and integration tests (Playwright) should be placed in
structured subdirectories that mirror the `src` tree.
